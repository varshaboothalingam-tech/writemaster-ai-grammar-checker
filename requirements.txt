# --- WriteMaster AI Grammar Checker ---
# Core web framework
flask>=2.3.0

# Deterministic rule layer (offline fallback)
spacy>=3.5.0
nltk>=3.8.0
pyspellchecker>=0.8.0

# Runtime config
python-dotenv>=1.0.0

# AI-first pipeline (Gemini via REST) uses only the standard library
# for the actual HTTP transport; requests is used by optional helpers/tests.
requests>=2.31.0

# --- Development / test ---
pytest>=8.0.0