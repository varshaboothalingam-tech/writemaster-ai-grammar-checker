# Copy to .env and fill in. NEVER commit .env.
# Gemini API key enables the AI brain (analysis + verification). Without it,
# the app runs in conservative offline mode.
GEMINI_API_KEY=

# Optional: force a specific provider ("gemini" only today)
AI_PROVIDER=